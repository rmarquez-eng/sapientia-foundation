{
  "name": "sapientia-foundation",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python3 .build/build.py",
    "test": "python3 .build/test_build.py",
    "dev": "npm run build && wrangler pages dev .",
    "deploy": "npm run build && wrangler pages deploy ."
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "unpdf": "^1.6.2"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
